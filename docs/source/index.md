# django-fundamentals

A reusable base Django app: custom user model, `django-allauth` +
`dj-rest-auth` authentication, `django-guardian` object-level permissions,
and an HTMX + Alpine.js + Tailwind CSS frontend baseline.

```{toctree}
:maxdepth: 2

quickstart
ui
email
usage
```
